import {StyleSheet} from 'react-native';
import {fontSize, space} from '../../styles/global';

export const styles = StyleSheet.create({
  text: {
    fontSize: fontSize.bodyText,
    padding: space.xs,
  },
});
